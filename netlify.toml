[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Refresh and data-read endpoints are deliberately unauthenticated (public, read-mostly
# market/news data, no personal data) — see disclaimer for the data-privacy rationale.
